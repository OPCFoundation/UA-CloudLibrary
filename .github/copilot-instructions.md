# Copilot Instructions

## Project Guidelines
- Always keep interface declarations and their implementations in separate .cs files, and place each file in the project folder that fits its role.
- Never commit or push changes to git directly. Always make the code changes, verify them (build/tests), and then stop and let the user review before any check-in. The user performs the commit and push themselves.