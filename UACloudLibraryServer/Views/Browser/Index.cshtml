@using AdminShell
@using Opc.Ua.Cloud.Library.Components.Pages
@using Microsoft.AspNetCore.Http

@{
    ViewData["Title"] = "Browser";
}

@model BrowserModel

@(await Html.RenderComponentAsync<TreePage>(RenderMode.ServerPrerendered, new { NodesetIdentifier = Model.nodesetIdentifier }).ConfigureAwait(false))
